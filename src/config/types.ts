interface ListItem {
  label: string;
  value: string;
}
interface SimpleStaff {
  approverGroup?: string;
  department?: string;
  email: string;
  name: string;
  staffId: string;
  role?: string;
}